---
name: test includes
description: used for variables storage

variables:
  service:
    proto: http
    host: localhost
    port: 5000
    mock_base_url: http://qainterview.cogniance.com/candidates